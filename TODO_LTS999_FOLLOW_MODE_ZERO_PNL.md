# TODO: LTS999 (และ MODE=follow legs อื่นๆ) ปิดไม้ PnL ≈ $0 บ่อยผิดปกติ

สถานะ: **ยังไม่แก้** — บันทึกไว้ก่อน (2026-08-26)

## อาการ

Profile `demo-iux-2101182460` (รัน `LTS999`) — เช็ค deal history ย้อนหลัง 10 วัน (magic=992999):
- ทั้งหมด 254 deals, ปิดด้วย PnL = $0.00 พอดี **11 ตัว (~4.3%)**
- ทุกตัว `reason=DEAL_REASON_TP` (ชน TP จริง) แต่ **ราคา fill = ราคา TP เป๊ะ**
- บางตัวปิดห่างจากเปิดแค่ไม่กี่วินาที (ตัวอย่างเร็วสุด: fill กับปิด **วินาทีเดียวกัน**)

ตัวอย่าง leg ที่เจอ: `c28` (7/11), `c3057` (2/11), `c889` (2/11) — ทั้งหมด M15, ทั้ง DIRECT/INVERSE, RD-band ต่างกันหลายช่วง (1.3-4.0)

## หลักฐาน (ticket 573514529, leg `LTS999_36` = `DIRECT_S84c28_RD2.7-3.4_H10`)

- Order วางตอน 11:34:22 → SL=4387.17, TP=4394.07
- Order ปิดตอน 11:34:40 (ห่างกัน 18 วิ) ที่ราคา **4394.07 พอดี** = TP
- risk (SL-distance) จริง = 6.90 — **ไม่ตรงกับ RD-band ของ leg นี้ (2.7-3.4) เลย**

## Root cause (ยืนยันจากโค้ด)

1. `c28`/`c110` (และ config อื่นที่ `MODE="follow"`) ใช้ [strategy84.py:218](strategy84.py:218) `_make_follow_result()` ซึ่งตั้ง `"order_mode": "market"` — วางเป็น **market order ทันที**
2. `entry` ที่ใช้คำนวณ SL/TP = ราคาปิดของแท่งสัญญาณ **ตอนตรวจจับ** ไม่ใช่ราคาตลาด ณ วินาทีที่ order จริงยิงออก
3. `_demo_scan_af_ladder()` ต้องวน scan เป็นร้อยๆ leg ต่อรอบ (LTS_AVENGERS-family มี ~900 legs/พอร์ต, มี `asyncio.sleep(0)` ทุก 20 leg กันบล็อก event loop) — สัญญาณของ leg หนึ่งอาจถูกตรวจจับตั้งแต่ต้นรอบ scan แต่กว่าจะถึงคิววาง order จริงอาจผ่านไปหลายวินาทีถึงเป็นนาที
4. ราคาตลาดขยับไปในช่วงนั้น พอ market order ยิงจริง ราคาที่ fill ได้ขยับมาใกล้/ชน TP ที่คำนวณไว้ล่วงหน้าแล้วพอดี → เปิดปุ๊บ TP ปั๊บ กำไร ≈ $0

RD-band filter ที่ [strategy_af.py:449](strategy_af.py:449) (`risk_distance = abs(entry - sl)`) เช็คแค่ตอนตรวจจับสัญญาณเท่านั้น ไม่ได้ผิดพลาด — ปัญหาคือช่วงเวลาระหว่างตรวจจับกับ fill จริงที่ราคาขยับไปแล้ว

## แนวทางแก้ที่เสนอไว้ (ยังไม่ทำ)

Re-check ราคาปัจจุบันเทียบกับ TP ก่อนส่ง market order จริง สำหรับ leg ที่ `MODE=follow` — ถ้าราคาใกล้/ผ่าน TP ไปแล้วให้ skip แทนที่จะยิง order ทิ้งเปล่าๆ

## ขอบเขตที่ยังไม่เช็ค

- ยังไม่เช็คว่า portfolio อื่นที่ใช้ leg สูตรเดียวกัน (LTS_AVENGERS_BASE, LTS890 ฯลฯ) เจอปัญหานี้เท่ากันไหม (มีสูตรซ้ำกันจาก leg pool เดียวกัน น่าจะเจอเหมือนกัน)
- ยังไม่ประเมินว่าอัตรา ~4.3% นี้กระทบ PF/edge โดยรวมมากแค่ไหน (แค่เสียโอกาสกำไร ไม่ใช่ขาดทุนเพิ่ม เพราะปิดที่ $0 ไม่ใช่ติดลบ)
