# CLAUDE.md

ไฟล์นี้คงไว้เพื่อ backward compatibility

## ใช้อะไรเป็นหลัก

- ให้ยึด [AGENTS.md](D:/Project/Copter01_AI_Bot_2/AGENTS.md) เป็นไฟล์หลักของ repository นี้
- ถ้าข้อมูลในไฟล์นี้ขัดกับ `AGENTS.md` ให้เชื่อ `AGENTS.md`

## สรุปสั้น

- อ่าน `AGENTS.md` ก่อนเริ่มงาน
- เปิดเฉพาะไฟล์ที่เกี่ยวกับงาน
- ระวัง encoding เป็น `UTF-8` และ line ending เป็น `LF`
- หลังแก้ไฟล์ให้รัน `python check_mojibake.py` และ `python verify_repo.py` เมื่อเหมาะสม
