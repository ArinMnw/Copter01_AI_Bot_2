# ท่าที่ 16 — AMD x iFVG (Asian Range Sweep + Inversion FVG)

| | |
|---|---|
| ไฟล์หลัก | `strategy16.py` |
| ประเภท | standalone — รันเฉพาะ **M1** |
| Default | OFF (ตั้งแต่ 11/06/2026) |

> ⚠️ order จริง 08-10/06: **−510.54 USD จาก 35 ไม้** (WR 42.9%) + sim A/B ติดลบทุก config
> เปิดใหม่ได้ผ่าน Telegram ถ้าอยากเก็บข้อมูล แต่ควรใช้ lot เล็กสุด

## แนวคิด (Accumulation–Manipulation–Distribution + Inversion FVG)

- **Asian Range**: คำนวณ High/Low จากช่วง `08:00–12:00 BKK` (ตีกรอบหลัง 12:00)
- **Killzones**: เทรดเฉพาะ London `14:00–17:00` และ NY `19:00–22:00` BKK
- **Sweep**: ราคาใน killzone ทะลุ Asian_Low (→ BUY) หรือ Asian_High (→ SELL)
- **Inversion FVG**: หลัง sweep ราคาพุ่งกลับ ปิดผ่าน FVG ฝั่งตรงข้าม → iFVG กลายเป็น entry zone
- **Entry**: LIMIT ที่ขอบ iFVG หรือ midline (`S16_ENTRY_MODE`)
- **SL**: ใต้/เหนือจุด sweep + `SL_BUFFER(atr)`
- **TP**: ขอบ Asian ฝั่งตรงข้าม หรือ fallback RR `S16_MIN_RR` (default 1.5)

## Fixes 11/06/2026 (จาก order จริง)

1. **One-shot dedup ต่อ (tf, side, killzone)** — `s16_state["fired"]` key `tf|side|kz_start`, persist ข้าม restart, prune > 2 วัน (เคส 09/06 มี SELL 13 ไม้ fill วินาทีเดียว −226 USD เพราะ dup check เทียบ entry/tp ที่ drift ตาม ATR)
2. **SL buffer ของตัวเอง** `S16_SL_ATR_BUFFER = 0.5` (เดิม SL_BUFFER กลาง = 2×ATR) + `S16_MAX_RISK_ATR_MULT = 4.0` skip setup risk กว้าง

## Standalone behavior

- bypass Trend Filter (scan) + Sweep Filter block
- skip: Fill Trend Recheck, RSI Recheck, Entry Candle, Trail SL, Opposite Order, Limit Guard, Near Approach Cancel, PD Fibo pre-check
- อยู่ใน `STRONG_TREND_BLOCK_SIDS`

## Config

`S16_KILLZONES`, `S16_ASIAN_START_BKK="08:00"`, `S16_ASIAN_END_BKK="12:00"`, `S16_MIN_RR=1.5`,
`S16_ENTRY_MODE`, `S16_SL_ATR_BUFFER=0.5`, `S16_MAX_RISK_ATR_MULT=4.0`, `S16_KZ_ONE_SHOT=True`

State `s16_state` (asian_high/low, range_date, swept_high/low, **fired**) → persist ผ่าน `save_runtime_state()`
